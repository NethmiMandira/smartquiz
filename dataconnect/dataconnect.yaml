specVersion: "v1"
serviceId: "myfirst"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "myfirst-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
